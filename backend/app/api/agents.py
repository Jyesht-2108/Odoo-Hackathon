"""
Placeholder router for the LangGraph agent orchestrator (teammate 3).

Teammate 3 will implement the agent-related endpoints here.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/agents", tags=["agents"])
